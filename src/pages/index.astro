---
import SiteLayout from "../layouts/SiteLayout.astro";

const base = import.meta.env.BASE_URL.endsWith("/")
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`;
const at = (path: string) => `${base}${path.replace(/^\//, "")}`;
const title = "ParqDB — Billion-scale vector search, built on Parquet";
const description =
  "ParqDB is an embedded vector database for larger-than-memory search and analytics, powered by Parquet and Arrow.";
---

<SiteLayout title={title} description={description}>
  <section class="hero">
    <div class="hero-grid shell">
      <div class="hero-copy">
        <div class="release-pill">
          <span class="release-dot"></span>
          <span>Open source · Python 3.11–3.14</span>
          <a href="https://pypi.org/project/parqdb/">View on PyPI <span aria-hidden="true">↗</span></a>
        </div>
        <h1>Billion-scale vector search, <span>built on Parquet.</span></h1>
        <p>
          ParqDB is an embedded vector database for larger-than-memory search
          and analytics, powered by Parquet and Arrow.
        </p>
        <div class="hero-actions">
          <a class="button button-primary" href={at("/docs/getting-started")}>Get started <span aria-hidden="true">→</span></a>
          <a class="button button-secondary" href="https://github.com/parqdb-io/parqdb">
            <svg viewBox="0 0 24 24" aria-hidden="true"><path fill="currentColor" d="M12 .7a11.5 11.5 0 0 0-3.64 22.4c.58.1.79-.25.79-.56v-2.23c-3.22.7-3.9-1.37-3.9-1.37-.52-1.34-1.28-1.7-1.28-1.7-1.05-.72.08-.7.08-.7 1.16.08 1.77 1.19 1.77 1.19 1.03 1.77 2.7 1.26 3.36.96.1-.75.4-1.26.73-1.55-2.57-.29-5.27-1.28-5.27-5.69 0-1.26.45-2.28 1.19-3.09-.12-.29-.52-1.46.11-3.05 0 0 .97-.31 3.16 1.18A10.9 10.9 0 0 1 12 6.1c.98 0 1.95.13 2.87.39 2.2-1.49 3.16-1.18 3.16-1.18.63 1.59.23 2.76.11 3.05.74.81 1.19 1.83 1.19 3.09 0 4.42-2.71 5.39-5.29 5.68.42.36.79 1.06.79 2.14v3.18c0 .31.21.67.8.56A11.5 11.5 0 0 0 12 .7Z"/></svg>
            Star on GitHub <span aria-hidden="true">↗</span>
          </a>
        </div>
        <div class="install-line" aria-label="Install command">
          <span class="prompt">$</span>
          <code>pip install parqdb</code>
          <button class="copy-command" data-copy="pip install parqdb" aria-label="Copy install command">
            <svg class="copy-icon" viewBox="0 0 20 20" aria-hidden="true"><rect x="6" y="6" width="10" height="10" rx="2" fill="none" stroke="currentColor"/><path d="M4 13H3a2 2 0 0 1-2-2V3a2 2 0 0 1 2-2h8a2 2 0 0 1 2 2v1" fill="none" stroke="currentColor"/></svg>
            <svg class="check-icon" viewBox="0 0 20 20" aria-hidden="true"><path d="m3 10 4 4 10-9" fill="none" stroke="currentColor" stroke-width="2"/></svg>
          </button>
        </div>
        <div class="hero-index" aria-label="ParqDB system properties">
          <span><b>FORMAT</b> PARQUET</span>
          <span><b>ENGINE</b> ARROW</span>
          <span><b>RUNTIME</b> EMBEDDED</span>
        </div>
      </div>

      <div class="query-card" aria-label="One open Parquet index shared by embedded, client-server, and browser runtimes">
        <div class="query-card-head">
          <div><i></i><i></i><i></i></div>
          <span>ONE INDEX / THREE RUNTIMES</span>
          <span class="live"><b></b> OPEN</span>
        </div>
        <div class="architecture-message">
          <strong>ONE PARQUET INDEX.<br /><span>QUERY IT ANYWHERE.</span></strong>
        </div>
        <div class="architecture-tabs" role="tablist" aria-label="Architecture mode">
          <button id="architecture-tab-embedded" role="tab" aria-selected="true" aria-controls="architecture-panel-embedded" data-architecture-tab="embedded"><b>01</b> Embedded</button>
          <button id="architecture-tab-server" role="tab" aria-selected="false" aria-controls="architecture-panel-server" data-architecture-tab="server" tabindex="-1"><b>02</b> Client / Server</button>
          <button id="architecture-tab-browser" role="tab" aria-selected="false" aria-controls="architecture-panel-browser" data-architecture-tab="browser" tabindex="-1"><b>03</b> Browser</button>
        </div>

        <div class="architecture-stage" data-architecture-stage data-mode="embedded">
          <div class="runtime-stage">
            <section id="architecture-panel-embedded" class="architecture-scene" role="tabpanel" aria-labelledby="architecture-tab-embedded" data-architecture-panel="embedded">
              <pre class="ascii-runtime" aria-label="ParqDB embedded in the application process with a local page cache">┌──────── APP PROCESS ────────┐<span class="ascii-shadow">░</span>
│                             │<span class="ascii-shadow">░</span>
│  <span class="ascii-engine">PARQDB</span>                     │<span class="ascii-shadow">░</span>
│  ┌───────────────────────┐  │<span class="ascii-shadow">░</span>
│  │      <span class="ascii-cache">PAGE CACHE</span>       │  │<span class="ascii-shadow">░</span>
│  └───────────────────────┘  │<span class="ascii-shadow">░</span>
│                             │<span class="ascii-shadow">░</span>
└─────────────────────────────┘<span class="ascii-shadow">░</span>
 <span class="ascii-shadow">░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░</span></pre>
            </section>

            <section id="architecture-panel-server" class="architecture-scene" role="tabpanel" aria-labelledby="architecture-tab-server" data-architecture-panel="server" hidden>
              <pre class="ascii-runtime" aria-label="A client querying ParqDB server with a server-side page cache"><span class="ascii-client">CLIENT</span> ── API ──▶
       ┌──── PARQDB SERVER ────┐<span class="ascii-shadow">░</span>
       │                       │<span class="ascii-shadow">░</span>
       │  <span class="ascii-engine">PARQDB</span>               │<span class="ascii-shadow">░</span>
       │  ┌─────────────────┐  │<span class="ascii-shadow">░</span>
       │  │   <span class="ascii-cache">PAGE CACHE</span>    │  │<span class="ascii-shadow">░</span>
       │  └─────────────────┘  │<span class="ascii-shadow">░</span>
       └───────────────────────┘<span class="ascii-shadow">░</span>
        <span class="ascii-shadow">░░░░░░░░░░░░░░░░░░░░░░░░░</span></pre>
            </section>

            <section id="architecture-panel-browser" class="architecture-scene" role="tabpanel" aria-labelledby="architecture-tab-browser" data-architecture-panel="browser" hidden>
              <pre class="ascii-runtime" aria-label="ParqDB WebAssembly in a browser tab with a browser page cache">┌──────── BROWSER TAB ────────┐<span class="ascii-shadow">░</span>
│  ●  ───────────────────     │<span class="ascii-shadow">░</span>
│                             │<span class="ascii-shadow">░</span>
│  <span class="ascii-engine">PARQDB WASM</span>                │<span class="ascii-shadow">░</span>
│  ┌───────────────────────┐  │<span class="ascii-shadow">░</span>
│  │      <span class="ascii-cache">PAGE CACHE</span>       │  │<span class="ascii-shadow">░</span>
│  └───────────────────────┘  │<span class="ascii-shadow">░</span>
└─────────────────────────────┘<span class="ascii-shadow">░</span>
 <span class="ascii-shadow">░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░</span></pre>
            </section>
          </div>

          <pre class="ascii-range ascii-range-horizontal" aria-hidden="true">── RANGE ──▶
◀═ <span>BYTES</span> ═══</pre>
          <pre class="ascii-range ascii-range-vertical" aria-hidden="true"> RANGE
   │
   ▼
   ▲
   │
 BYTES</pre>

          <pre class="ascii-storage" aria-label="Parquet files with pruned row groups and selected column pages">┌─── <span class="ascii-engine">OBJECT STORAGE</span> ───────────┐<span class="ascii-shadow">░</span>
│  FILE 00  <span class="ascii-file">[FILE 01]</span>  FILE 02 │<span class="ascii-shadow">░</span>
│              ╰─────╮         │<span class="ascii-shadow">░</span>
│ ┌─ <span class="ascii-file">FILE 01.parquet</span> ────────┐ │<span class="ascii-shadow">░</span>
│ │ ┌─ ROW GROUP 00 ───────┐ │ │<span class="ascii-shadow">░</span>
│ │ │ ░░░░░░░░░░░░░░░░░░░░ │ │ │<span class="ascii-shadow">░</span>
│ │ ├─ <span class="ascii-selected">ROW GROUP 01</span> ───────┤ │ │<span class="ascii-shadow">░</span>
│ │ │  CID │ KEY │  LVQ8   │ │ │<span class="ascii-shadow">░</span>
│ │ │  ░░░ │ ░░░ │ ░<span class="ascii-read">██</span>░<span class="ascii-read">█</span>░  │ │ │<span class="ascii-shadow">░</span>
│ │ ├─ ROW GROUP 02 ───────┤ │ │<span class="ascii-shadow">░</span>
│ │ │ ░░░░░░░░░░░░░░░░░░░░ │ │ │<span class="ascii-shadow">░</span>
│ │ └────── <span class="ascii-footer">FOOTER</span> ────────┘ │ │<span class="ascii-shadow">░</span>
│ ╰───────\____/\_______/────╯ │<span class="ascii-shadow">░</span>
└──────────────────────────────┘<span class="ascii-shadow">░</span>
 <span class="ascii-shadow">░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░</span></pre>
        </div>
        <div class="story-footer">
          <span>ONE IMMUTABLE INDEX</span><span>READ ONLY THE HIGHLIGHTED BYTES</span>
        </div>
      </div>
    </div>
  </section>

  <section class="benchmark-bar" aria-label="SIFT1B benchmark highlights">
    <div class="benchmark-grid shell">
      <a href="https://github.com/parqdb-io/parqdb/tree/main/benchmarks/results/linux-x86_64-2026-08-17">
        <strong>1 billion</strong><span>vectors searched</span>
      </a>
      <div><strong>63.05 ms</strong><span>median latency</span></div>
      <div><strong>90.3%</strong><span>Recall@10</span></div>
      <div><strong>2 cores · 4 GB</strong><span>query resource limit</span></div>
    </div>
    <p class="benchmark-note shell">SIFT1B · IVF-LVQ8 · nprobe 32 · exact conditions and raw results are published with the benchmark.</p>
  </section>

  <section class="principles shell">
    <div class="section-intro">
      <span class="eyebrow">Why ParqDB</span>
      <h2 class="section-heading">The index is data.<br />The engine stays open.</h2>
      <p class="section-copy">Vector search should fit into your data system—not force every workload through another proprietary service.</p>
    </div>
    <div class="principle-grid">
      <article>
        <div class="feature-index">01</div>
        <div class="feature-icon layers" aria-hidden="true"><span></span><span></span><span></span></div>
        <h3>Everything is Parquet</h3>
        <p>Source data and vector indexes use standard Parquet. Version, publish, inspect, and share them with the tools you already use.</p>
        <a href={at("/docs/concepts")}>Explore the format <span aria-hidden="true">→</span></a>
      </article>
      <article>
        <div class="feature-index">02</div>
        <div class="feature-icon bounded" aria-hidden="true"><span></span><span></span></div>
        <h3>Scale beyond memory</h3>
        <p>Explicit cluster, file, row-group, and page pruning keeps search bounded while the index grows far beyond RAM.</p>
        <a href="https://github.com/parqdb-io/parqdb/tree/main/benchmarks/results">Read the benchmarks <span aria-hidden="true">→</span></a>
      </article>
      <article>
        <div class="feature-index">03</div>
        <div class="feature-icon relational" aria-hidden="true"><span></span><span></span><span></span></div>
        <h3>Search stays relational</h3>
        <p>Compose vector search with filters, joins, and aggregations in one DataFusion plan, backed by Arrow-native execution.</p>
        <a href={at("/docs/guides/embedded")}>Build a query <span aria-hidden="true">→</span></a>
      </article>
    </div>
  </section>

  <section class="architecture">
    <div class="shell">
      <div class="architecture-head">
        <div>
          <span class="eyebrow">One index, multiple runtimes</span>
          <h2 class="section-heading">Build once. Query where the work happens.</h2>
        </div>
        <p>ParqDB separates the open index format from the runtime that executes it.</p>
      </div>
      <div class="runtime-map">
        <div class="map-node source-node">
          <span class="node-type">SOURCE</span>
          <strong>Multimodal table</strong>
          <small>vectors + payload</small>
          <b>PARQUET</b>
        </div>
        <div class="map-connector"><span></span><b>BUILD</b></div>
        <div class="map-node index-node">
          <span class="node-type">OPEN INDEX</span>
          <strong>IVF / LVQ4 / LVQ8</strong>
          <small>centroids + postings</small>
          <b>PARQUET</b>
        </div>
        <div class="map-fanout">
          <span></span><span></span><span></span>
        </div>
        <div class="runtime-nodes">
          <div><b>EMBEDDED</b><strong>DataFusion</strong><small>in-process Python</small></div>
          <div><b>BROWSER</b><strong>WASM</strong><small>HTTP Range</small></div>
          <div class="experimental"><b>SERVER</b><strong>Arrow IPC</strong><small>experimental</small></div>
        </div>
      </div>
    </div>
  </section>

  <section class="quickstart shell">
    <div class="quickstart-copy">
      <span class="eyebrow">Five-minute start</span>
      <h2 class="section-heading">From Parquet to nearest neighbors.</h2>
      <p class="section-copy">Install one Python package. Build a persistent IVF index. Keep the result inside the Arrow ecosystem.</p>
      <ul>
        <li><b>01</b><span>Register an existing Parquet table</span></li>
        <li><b>02</b><span>Build a source-keyed vector index</span></li>
        <li><b>03</b><span>Filter, rank, and collect as Arrow</span></li>
      </ul>
      <a class="button button-primary" href={at("/docs/getting-started")}>Follow the guide <span aria-hidden="true">→</span></a>
    </div>
    <div class="code-window">
      <div class="code-tabs" role="tablist" aria-label="Quickstart examples">
        <button role="tab" aria-selected="true" data-code-tab="python">Python</button>
        <button role="tab" aria-selected="false" data-code-tab="publish">Publish</button>
        <span>quickstart</span>
      </div>
      <pre data-code-panel="python"><code><span class="kw">import</span> parqdb

session = parqdb.<span class="fn">connect</span>(<span class="str">"./parqdb-data"</span>)
session.<span class="fn">register_parquet</span>(
    <span class="str">"documents"</span>,
    parqdb.datasets.<span class="fn">uri</span>(<span class="str">"documents"</span>),
)
documents = session.<span class="fn">table</span>(<span class="str">"documents"</span>)

documents.<span class="fn">create_index</span>(
    <span class="str">"documents_embedding"</span>,
    column=<span class="str">"embedding"</span>,
    key=[<span class="str">"document_id"</span>],
    config=parqdb.<span class="fn">IVF</span>(nlist=<span class="num">3</span>),
)
documents.<span class="fn">wait_for_index</span>(<span class="str">"documents_embedding"</span>)

hits = session.<span class="fn">collect</span>(
    documents.<span class="fn">search</span>([<span class="num">0.2</span>, <span class="num">0.0</span>])
    .<span class="fn">where</span>(<span class="str">"status = 'published'"</span>)
    .<span class="fn">limit</span>(<span class="num">10</span>)
)</code></pre>
      <pre data-code-panel="publish" hidden><code><span class="prompt">$</span> pip install <span class="str">"parqdb[publish]"</span>

<span class="prompt">$</span> parqdb publish \
    --source documents.parquet \
    --key chunk_id \
    --vector-column embedding \
    --nlist <span class="num">4096</span> \
    --encoding lvq8 \
    --destination s3://my-bucket/wiki/v1 \
    --public-url https://data.example.com/wiki/v1

<span class="comment"># manifest.json is exposed only after every
# immutable object has been uploaded and verified.</span></code></pre>
    </div>
  </section>

  <section class="browser-feature shell">
    <div class="browser-panel">
      <div class="browser-copy">
        <span class="eyebrow">Static HTTP index</span>
        <h2>Object storage becomes the query endpoint.</h2>
        <p>Publish an immutable IVF-LVQ index and query only the required Parquet byte ranges in WebAssembly. No query server sits between the browser and your data.</p>
        <div class="browser-actions">
          <a class="button button-primary" href="https://search.parqdb.io/">Run the live demo <span aria-hidden="true">↗</span></a>
          <a href={at("/docs/guides/browser")}>Publication guide <span aria-hidden="true">→</span></a>
        </div>
      </div>
      <div class="http-trace" aria-label="Example HTTP Range trace">
        <div class="trace-header"><span>HTTP TRACE</span><span><b></b> LIVE INDEX</span></div>
        <div class="trace-row"><b>RANGE</b><span>centroids.parquet</span><code>0–262143</code><em>206</em></div>
        <div class="trace-row"><b>HIT</b><span>roots.parquet</span><code>memory cache</code><em>CACHE</em></div>
        <div class="trace-row"><b>RANGE</b><span>cid_bucket=000041</span><code>8630–144983</code><em>206</em></div>
        <div class="trace-row"><b>RANGE</b><span>documents.parquet</span><code>17465660–17483301</code><em>206</em></div>
        <div class="trace-footer"><span>QUERY TEXT NEVER LEAVES THIS TAB</span><span>WASM / LVQ8</span></div>
      </div>
    </div>
  </section>

  <section class="final-cta shell">
    <span class="eyebrow">Start in process</span>
    <h2>Keep your vectors in an open format.</h2>
    <p>Install ParqDB and build your first index from an existing Parquet table.</p>
    <div>
      <a class="button button-primary" href={at("/docs/getting-started")}>Read the docs <span aria-hidden="true">→</span></a>
      <a class="button button-secondary" href="https://github.com/parqdb-io/parqdb">View GitHub <span aria-hidden="true">↗</span></a>
    </div>
  </section>

  <script>
    document.querySelectorAll(".copy-command").forEach((button) => {
      button.addEventListener("click", async () => {
        if (!(button instanceof HTMLElement)) return;
        await navigator.clipboard.writeText(button.dataset.copy ?? "");
        button.classList.add("copied");
        window.setTimeout(() => button.classList.remove("copied"), 1400);
      });
    });

    document.querySelectorAll("[data-code-tab]").forEach((tab) => {
      tab.addEventListener("click", () => {
        if (!(tab instanceof HTMLElement)) return;
        const selected = tab.dataset.codeTab;
        document.querySelectorAll("[data-code-tab]").forEach((item) =>
          item.setAttribute("aria-selected", String(item === tab)),
        );
        document.querySelectorAll("[data-code-panel]").forEach((panel) => {
          if (panel instanceof HTMLElement) panel.hidden = panel.dataset.codePanel !== selected;
        });
      });
    });

    const architectureTabs = Array.from(document.querySelectorAll("[data-architecture-tab]"));
    const activateArchitecture = (tab: Element) => {
      if (!(tab instanceof HTMLButtonElement)) return;
      const selected = tab.dataset.architectureTab;
      architectureTabs.forEach((item) => {
        const active = item === tab;
        item.setAttribute("aria-selected", String(active));
        if (item instanceof HTMLButtonElement) item.tabIndex = active ? 0 : -1;
      });
      document.querySelectorAll("[data-architecture-panel]").forEach((panel) => {
        if (panel instanceof HTMLElement) panel.hidden = panel.dataset.architecturePanel !== selected;
      });
      const stage = document.querySelector("[data-architecture-stage]");
      if (stage instanceof HTMLElement && selected) stage.dataset.mode = selected;
    };

    architectureTabs.forEach((tab, index) => {
      tab.addEventListener("click", () => activateArchitecture(tab));
      tab.addEventListener("keydown", (event) => {
        if (!(event instanceof KeyboardEvent)) return;
        let next = index;
        if (event.key === "ArrowRight") next = (index + 1) % architectureTabs.length;
        else if (event.key === "ArrowLeft") next = (index - 1 + architectureTabs.length) % architectureTabs.length;
        else if (event.key === "Home") next = 0;
        else if (event.key === "End") next = architectureTabs.length - 1;
        else return;
        event.preventDefault();
        const nextTab = architectureTabs[next];
        activateArchitecture(nextTab);
        if (nextTab instanceof HTMLButtonElement) nextTab.focus();
      });
    });
  </script>
</SiteLayout>

<style>
  .hero { position: relative; overflow: hidden; padding: 88px 0 94px; border-bottom: 1px solid #3b454c; background: #090c10; }
  .hero::before { content: ""; position: absolute; inset: 0; pointer-events: none; background-image: linear-gradient(rgba(82,229,255,.08) 1px, transparent 1px), linear-gradient(90deg, rgba(82,229,255,.08) 1px, transparent 1px); background-size: 48px 48px; mask-image: linear-gradient(to right, black, rgba(0,0,0,.22)); }
  .hero::after { content: "PARQUET / OPEN INDEX / 00000001"; position: absolute; right: 18px; bottom: 10px; color: #4f5b62; font-family: monospace; font-size: 9px; letter-spacing: .14em; writing-mode: vertical-rl; }
  .hero-grid { position: relative; display: grid; grid-template-columns: minmax(0, .95fr) minmax(0, 1.05fr); align-items: center; gap: 44px; width: min(1320px, calc(100% - 40px)); }
  .release-pill { display: inline-flex; align-items: center; gap: 9px; padding: 7px 8px 7px 10px; border: 1px solid #566169; border-radius: 1px; color: var(--text-2); background: #11171d; font-family: monospace; font-size: 10px; letter-spacing: .035em; box-shadow: 3px 3px 0 #202a31; }
  .release-pill a { margin-left: 4px; padding: 4px 7px; color: var(--ink); background: var(--cyan); text-decoration: none; }
  .release-dot { width: 7px; height: 7px; background: var(--green); animation: statusBlink 2.2s steps(2, end) infinite; }
  h1 { max-width: 720px; margin: 30px 0 0; color: var(--white); font-size: clamp(54px, 6.3vw, 86px); font-weight: 850; line-height: .91; letter-spacing: -.063em; }
  h1 span { color: var(--green); text-shadow: 4px 4px 0 #175067; }
  .hero-copy > p { max-width: 630px; margin: 29px 0 0; color: var(--text-2); font-size: clamp(17px, 2vw, 20px); line-height: 1.58; }
  .hero-actions { display: flex; flex-wrap: wrap; gap: 15px; margin-top: 35px; }
  .hero-actions svg { width: 17px; height: 17px; }
  .install-line { display: flex; width: min(100%, 390px); align-items: center; margin-top: 25px; padding: 10px 11px; border: 1px solid #4a555d; border-radius: 1px; color: #d2ddd8; background: #0c1116; box-shadow: inset 3px 0 var(--blue); font-size: 12px; }
  .install-line .prompt { margin-right: 10px; color: var(--green); }
  .copy-command { width: 28px; height: 28px; margin-left: auto; padding: 5px; border: 1px solid transparent; color: var(--text-3); background: transparent; cursor: pointer; }
  .copy-command:hover { color: var(--cyan); border-color: #39434a; }
  .copy-command svg { width: 16px; height: 16px; }
  .check-icon, .copy-command.copied .copy-icon { display: none; }
  .copy-command.copied .check-icon { display: block; color: var(--green); }
  .hero-index { display: flex; flex-wrap: wrap; gap: 8px 20px; margin-top: 25px; color: #65736f; font-family: monospace; font-size: 9px; letter-spacing: .08em; }
  .hero-index b { color: #a5b2ad; font-weight: 500; }
  .query-card { position: relative; min-width: 0; padding: 5px 0; }
  .query-card::before { content: "SYS"; position: absolute; top: -17px; right: 0; padding: 5px 8px; color: var(--ink); background: var(--cyan); font-family: monospace; font-size: 9px; font-weight: 800; letter-spacing: .12em; }
  .query-card-head { display: grid; grid-template-columns: 1fr auto 1fr; align-items: center; padding: 12px 0; color: #71807b; font-family: monospace; font-size: 9px; letter-spacing: .08em; }
  .query-card-head > div { display: flex; gap: 4px; }
  .query-card-head i { width: 7px; height: 7px; background: #3b474d; }
  .query-card-head i:first-child { background: var(--orange); }
  .query-card-head i:nth-child(2) { background: var(--green); }
  .query-card-head .live { justify-self: end; color: var(--green); }
  .live b { display: inline-block; width: 6px; height: 6px; margin-right: 5px; background: currentColor; }
  .architecture-message { margin: 12px 0 18px; }
  .architecture-message strong { display: block; color: var(--white); font-size: clamp(20px, 2vw, 27px); font-weight: 850; line-height: 1.03; letter-spacing: -.045em; }
  .architecture-message strong span { color: var(--green); }
  .architecture-tabs { display: grid; grid-template-columns: repeat(3, 1fr); margin: 7px 0 22px; border-bottom: 1px solid #3d484e; font-family: "SFMono-Regular", Consolas, "Liberation Mono", monospace; }
  .architecture-tabs button { position: relative; padding: 12px 8px 11px; border: 0; border-bottom: 2px solid transparent; color: #778681; background: transparent; font: inherit; font-size: 9px; font-weight: 700; letter-spacing: .055em; cursor: pointer; text-transform: uppercase; }
  .architecture-tabs button:hover { color: #d7e1dd; }
  .architecture-tabs button:focus-visible { outline: 2px solid var(--cyan); outline-offset: -2px; }
  .architecture-tabs button[aria-selected="true"] { border-bottom-color: var(--green); color: var(--white); background: rgba(182, 243, 74, .045); }
  .architecture-tabs button b { margin-right: 7px; color: var(--green); font-size: 7px; }
  .architecture-stage { display: grid; grid-template-columns: minmax(0, 1.25fr) 72px minmax(210px, .9fr); min-height: 260px; align-items: center; gap: 8px; isolation: isolate; font-family: "SFMono-Regular", Consolas, "Liberation Mono", monospace; }
  .runtime-stage, .architecture-scene { min-width: 0; }
  .runtime-stage { display: grid; min-height: 230px; place-items: center; }
  .architecture-scene { animation: sceneIn 160ms ease-out; }
  .architecture-scene[hidden] { display: none; }
  .ascii-runtime, .ascii-storage, .ascii-range { overflow: visible; margin: 0; padding: 0; border: 0; border-radius: 0; color: #aebbb6; background: transparent; font-family: "SFMono-Regular", Consolas, "Liberation Mono", monospace; font-size: clamp(8.5px, .72vw, 10px); font-variant-ligatures: none; font-weight: 600; line-height: 1.48; letter-spacing: 0; white-space: pre; }
  .ascii-runtime { width: max-content; max-width: 100%; color: #d4dcd8; font-size: clamp(9.5px, .8vw, 11px); }
  .ascii-storage { justify-self: end; color: #7f8d88; font-size: clamp(8px, .68vw, 9.5px); }
  .ascii-engine, .ascii-file { color: var(--cyan); font-weight: 800; }
  .ascii-cache, .ascii-selected, .ascii-footer { color: var(--green); font-weight: 800; }
  .ascii-read { color: #081009; background: var(--green); box-shadow: 0 0 8px rgba(182,243,74,.4); }
  .ascii-client { color: var(--orange); font-weight: 800; }
  .ascii-shadow { color: #42545d; font-weight: 400; }
  .ascii-storage .ascii-shadow { color: #58772f; }
  .ascii-range { justify-self: stretch; color: #87958f; font-size: 6px; line-height: 2.1; text-align: center; }
  .ascii-range span { color: var(--cyan); font-weight: 800; }
  .ascii-range-vertical { display: none; }
  @keyframes sceneIn { from { opacity: 0; transform: translateY(5px); } to { opacity: 1; transform: translateY(0); } }
  .story-footer { display: flex; justify-content: space-between; padding: 13px 0 0; color: #71817b; font-family: monospace; font-size: 8px; letter-spacing: .09em; }
  .benchmark-bar { padding: 32px 0 23px; border-block: 1px solid var(--ink); color: var(--ink); background: var(--green); }
  .benchmark-grid { display: grid; grid-template-columns: repeat(4, 1fr); }
  .benchmark-grid > * { padding: 0 28px; border-right: 1px solid rgba(10,14,18,.35); color: var(--ink); text-decoration: none; }
  .benchmark-grid > *:first-child { padding-left: 0; }
  .benchmark-grid > *:last-child { border: 0; }
  .benchmark-grid strong { display: block; color: var(--ink); font-size: 25px; font-weight: 850; letter-spacing: -.04em; }
  .benchmark-grid span { display: block; margin-top: 4px; color: #34402c; font-family: monospace; font-size: 10px; letter-spacing: .04em; text-transform: uppercase; }
  .benchmark-note { margin-top: 24px; color: #4e5b43; font-family: monospace; font-size: 9px; text-transform: uppercase; letter-spacing: .07em; }
  .principles { padding-block: 136px; color: var(--ink); background: var(--paper); box-shadow: 0 0 0 100vmax var(--paper); clip-path: inset(0 -100vmax); }
  .principles .eyebrow { color: #145ec4; }
  .principles .section-heading { color: var(--ink); }
  .principles .section-copy { color: #3f4947; }
  .principle-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 14px; margin-top: 68px; }
  .principle-grid article { position: relative; min-height: 400px; padding: 38px 30px 31px; border: 1px solid var(--ink); background: var(--paper-muted); transition: transform 140ms ease, border-color 140ms ease, background 140ms ease; }
  .principle-grid article:nth-child(1) { box-shadow: 10px 10px 0 #35a7ff; }
  .principle-grid article:nth-child(2) { box-shadow: 10px 10px 0 #8fbe31; }
  .principle-grid article:nth-child(3) { box-shadow: 10px 10px 0 #765ed2; }
  .principle-grid article:hover { border-color: #0b72d9; background: var(--paper); transform: translateY(-3px); }
  .feature-index { position: absolute; top: 15px; right: 17px; color: #56615e; font-family: monospace; font-size: 9px; }
  .feature-icon { position: relative; width: 54px; height: 54px; margin-bottom: 42px; }
  .feature-icon.layers span { position: absolute; left: 4px; width: 43px; height: 17px; border: 2px solid #0b72d9; transform: skewY(-13deg); background: rgba(53,167,255,.12); }
  .feature-icon.layers span:nth-child(2) { top: 14px; border-color: #664fc7; }
  .feature-icon.layers span:nth-child(3) { top: 28px; border-color: #0986a0; }
  .feature-icon.bounded { border: 2px solid var(--ink); }
  .feature-icon.bounded span:first-child { position: absolute; inset: 8px; border: 2px dashed #684fd0; }
  .feature-icon.bounded span:last-child { position: absolute; top: 18px; left: -5px; width: 62px; height: 14px; background: repeating-linear-gradient(90deg, #198cc8 0 7px, transparent 7px 10px); }
  .feature-icon.relational span { position: absolute; width: 13px; height: 13px; border: 3px solid #0c78d5; }
  .feature-icon.relational span:nth-child(2) { right: 0; top: 20px; border-color: #654fd1; }
  .feature-icon.relational span:nth-child(3) { bottom: 0; left: 7px; border-color: #17907f; }
  .feature-icon.relational::before, .feature-icon.relational::after { content: ""; position: absolute; height: 2px; background: #27302f; transform-origin: left; }
  .feature-icon.relational::before { width: 37px; top: 9px; left: 11px; transform: rotate(27deg); }
  .feature-icon.relational::after { width: 35px; top: 15px; left: 8px; transform: rotate(80deg); }
  article h3 { margin: 0; color: var(--ink); font-size: 23px; letter-spacing: -.03em; }
  article p { min-height: 110px; margin: 17px 0 25px; color: #3f4947; font-size: 15px; line-height: 1.7; }
  article a { color: #0b5fb7; font-family: monospace; font-size: 11px; font-weight: 700; text-decoration: none; text-transform: uppercase; }
  .architecture { padding: 128px 0 140px; border-block: 1px solid #3b454c; background: #0e141a; }
  .architecture-head { display: grid; grid-template-columns: 1fr 360px; align-items: end; gap: 64px; }
  .architecture-head > p { margin: 0 0 6px; color: var(--text-2); line-height: 1.7; }
  .runtime-map { display: grid; grid-template-columns: 1fr 130px 1fr 150px 1.2fr; align-items: center; margin-top: 74px; }
  .map-node { position: relative; min-height: 168px; padding: 23px; border: 1px solid #536169; border-radius: 0; background: #141c24; box-shadow: 6px 6px 0 #28343d; }
  .map-node strong, .map-node small { display: block; }
  .map-node strong { margin-top: 28px; color: var(--white); font-size: 18px; }
  .map-node small { margin-top: 6px; color: var(--text-3); }
  .map-node > b { position: absolute; right: 18px; bottom: 17px; color: #83958f; font-family: monospace; font-size: 9px; }
  .node-type { color: var(--cyan); font-family: monospace; font-size: 9px; letter-spacing: .12em; }
  .index-node { border-color: var(--green); box-shadow: 6px 6px 0 #42591d; }
  .map-connector { position: relative; height: 1px; background: #607079; }
  .map-connector::after { content: ""; position: absolute; top: -3px; right: -1px; border-left: 6px solid var(--cyan); border-block: 3px solid transparent; }
  .map-connector b { position: absolute; top: -21px; left: 50%; color: #7d8c88; font-family: monospace; font-size: 9px; transform: translateX(-50%); }
  .map-fanout { display: flex; flex-direction: column; justify-content: space-between; height: 150px; padding-block: 20px; border-left: 1px solid #607079; }
  .map-fanout span { display: block; width: 100%; height: 1px; background: #607079; }
  .runtime-nodes { display: grid; gap: 10px; }
  .runtime-nodes > div { display: grid; grid-template-columns: 70px 1fr auto; align-items: center; min-height: 50px; padding: 0 13px; border: 1px solid #49565d; border-radius: 0; background: #141c24; }
  .runtime-nodes b { color: var(--cyan); font-family: monospace; font-size: 8px; letter-spacing: .08em; }
  .runtime-nodes strong { color: #dbe5e0; font-size: 13px; }
  .runtime-nodes small { color: #788783; font-size: 9px; }
  .runtime-nodes .experimental b { color: var(--violet); }
  .quickstart { display: grid; grid-template-columns: .8fr 1.2fr; align-items: center; gap: 86px; padding-block: 144px; }
  .quickstart ul { margin: 35px 0 38px; padding: 0; list-style: none; }
  .quickstart li { display: flex; gap: 17px; padding: 13px 0; border-top: 1px solid #3b454c; color: var(--text-2); font-size: 14px; }
  .quickstart li:last-child { border-bottom: 1px solid #3b454c; }
  .quickstart li b { color: var(--green); font-family: monospace; font-size: 10px; }
  .code-window { overflow: hidden; border: 1px solid var(--cyan); border-radius: 0; background: #0b1116; box-shadow: 10px 10px 0 #173f50; }
  .code-tabs { display: flex; align-items: center; height: 48px; padding: 0 14px; border-bottom: 1px solid #3b454c; background: #131b22; }
  .code-tabs button { align-self: stretch; padding: 0 14px; border: 0; border-bottom: 3px solid transparent; color: #82948e; background: none; font-family: monospace; font-size: 10px; cursor: pointer; text-transform: uppercase; }
  .code-tabs button[aria-selected="true"] { color: var(--green); border-color: var(--green); }
  .code-tabs > span { margin-left: auto; color: #82948e; font-family: monospace; font-size: 8px; letter-spacing: .12em; }
  .code-window pre { overflow: auto; min-height: 540px; margin: 0; padding: 28px; color: #bdc9c4; font-size: 12px; line-height: 1.75; tab-size: 4; }
  .code-window .kw { color: #c999ff; }.code-window .fn { color: var(--cyan); }.code-window .str { color: var(--green); }.code-window .num { color: var(--orange); }.code-window .comment { color: #60716c; }.code-window .prompt { color: var(--violet); }
  .browser-feature { padding-bottom: 146px; }
  .browser-panel { position: relative; overflow: hidden; display: grid; grid-template-columns: .88fr 1.12fr; align-items: center; gap: 66px; min-height: 520px; padding: 62px; border: 1px solid var(--cyan); border-radius: 0; background: #102b54; box-shadow: 12px 12px 0 #335d1a, 13px 13px 0 var(--green); }
  .browser-panel::before { content: ""; position: absolute; inset: 0; opacity: .18; background-image: linear-gradient(rgba(82,229,255,.35) 1px, transparent 1px), linear-gradient(90deg, rgba(82,229,255,.35) 1px, transparent 1px); background-size: 32px 32px; }
  .browser-copy, .http-trace { position: relative; }
  .browser-copy h2 { max-width: 520px; margin: 18px 0 0; color: var(--white); font-size: clamp(39px, 4vw, 56px); line-height: .98; letter-spacing: -.052em; }
  .browser-copy p { margin: 22px 0 0; color: #c2d2dc; line-height: 1.7; }
  .browser-actions { display: flex; align-items: center; gap: 22px; margin-top: 32px; }
  .browser-actions > a:not(.button) { color: var(--cyan); font-family: monospace; font-size: 10px; font-weight: 700; text-decoration: none; text-transform: uppercase; }
  .http-trace { border: 1px solid var(--green); border-radius: 0; background: #091016; box-shadow: 7px 7px 0 #42591d; font-family: monospace; }
  .trace-header, .trace-footer { display: flex; justify-content: space-between; padding: 14px 16px; color: #71817b; font-size: 8px; letter-spacing: .1em; }
  .trace-header { border-bottom: 1px solid #3b454c; }.trace-footer { border-top: 1px solid #3b454c; }
  .trace-header b { display: inline-block; width: 6px; height: 6px; margin-right: 5px; background: var(--green); }
  .trace-row { display: grid; grid-template-columns: 50px 1fr 1.1fr 45px; gap: 10px; padding: 17px 16px; border-bottom: 1px solid #283239; font-size: 9px; }
  .trace-row b { color: var(--cyan); }.trace-row span { overflow: hidden; color: #acbbb6; text-overflow: ellipsis; white-space: nowrap; }.trace-row code { color: #71817b; }.trace-row em { justify-self: end; color: var(--green); font-style: normal; }
  .final-cta { padding: 136px 0 142px; color: var(--ink); text-align: center; background: var(--paper); box-shadow: 0 0 0 100vmax var(--paper); clip-path: inset(0 -100vmax); }
  .final-cta .eyebrow { justify-content: center; color: #145ec4; }
  .final-cta h2 { margin: 20px auto 0; color: var(--ink); font-size: clamp(44px, 6vw, 76px); line-height: .95; letter-spacing: -.055em; }
  .final-cta p { margin: 22px auto 0; color: #46514e; font-size: 18px; }
  .final-cta > div { display: flex; justify-content: center; gap: 15px; margin-top: 34px; }
  .final-cta .button-secondary { color: var(--ink); border-color: var(--ink); background: transparent; box-shadow: 5px 5px 0 #8f9b95; }

  @keyframes statusBlink { 0%, 70% { opacity: 1; } 71%, 100% { opacity: .25; } }

  @media (max-width: 980px) {
    .hero-grid { grid-template-columns: 1fr; }.query-card { max-width: 700px; }
    .principle-grid { grid-template-columns: 1fr; }.principle-grid article { min-height: 300px; border-right: 0; border-bottom: 1px solid var(--line); }.principle-grid article:last-child { border-bottom: 0; } article p { min-height: 0; }
    .architecture-head { grid-template-columns: 1fr; }.architecture-head > p { max-width: 650px; }
    .runtime-map { grid-template-columns: 1fr 80px 1fr; }.map-fanout { display: none; }.runtime-nodes { grid-column: 1 / -1; grid-template-columns: repeat(3, 1fr); margin-top: 40px; }.runtime-nodes > div { grid-template-columns: 1fr; gap: 4px; padding-block: 12px; }
    .quickstart { grid-template-columns: 1fr; }.quickstart-copy { max-width: 650px; }
    .browser-panel { grid-template-columns: 1fr; }.http-trace { max-width: 700px; }
  }
  @media (max-width: 700px) {
    .hero { padding: 64px 0 72px; }.hero-grid { gap: 52px; width: calc(100% - 28px); }.release-pill > span:not(.release-dot) { display: none; }
    h1 { font-size: clamp(46px, 15vw, 66px); }.hero-copy > p { font-size: 17px; }
    .query-card-head { grid-template-columns: 1fr 1fr; }.query-card-head > span:nth-child(2) { display: none; }
    .architecture-tabs { margin-bottom: 16px; }.architecture-tabs button { padding: 10px 3px 9px; font-size: 6.5px; }.architecture-tabs button b { margin-right: 3px; font-size: 5px; }
    .architecture-stage { grid-template-columns: 1fr; min-height: 0; gap: 13px; padding-top: 5px; }
    .runtime-stage { min-height: 160px; }.ascii-runtime { font-size: 10px; }.ascii-storage { justify-self: center; font-size: 9.5px; }.ascii-range-horizontal { display: none; }.ascii-range-vertical { display: block; font-size: 5px; line-height: 1.1; }.architecture-stage { gap: 9px; }
    .story-footer { gap: 8px; font-size: 7px; }
    .benchmark-grid { grid-template-columns: 1fr 1fr; gap: 28px 0; }.benchmark-grid > * { padding: 0 16px; }.benchmark-grid > *:nth-child(2) { border-right: 0; }.benchmark-grid > *:nth-child(3) { padding-left: 0; }
    .principles, .quickstart { padding-block: 96px; }.principle-grid { margin-top: 50px; }.principle-grid article { padding-inline: 18px; }
    .architecture { padding-block: 96px; }.runtime-map { grid-template-columns: 1fr; gap: 16px; }.map-connector { width: 1px; height: 45px; margin: 0 auto; }.map-connector::after { top: auto; right: -3px; bottom: -1px; border-top: 6px solid #6687b8; border-inline: 3px solid transparent; border-bottom: 0; }.map-connector b { top: 14px; left: 12px; transform: none; }.runtime-nodes { grid-column: auto; grid-template-columns: 1fr; margin-top: 10px; }
    .quickstart { gap: 52px; }.code-window pre { min-height: 0; padding: 20px; font-size: 10px; }
    .browser-feature { padding: 0 4px 100px 0; }.browser-panel { gap: 48px; min-height: auto; padding: 32px 20px; box-shadow: 7px 7px 0 #335d1a, 8px 8px 0 var(--green); }.trace-row { grid-template-columns: 45px 1fr 38px; }.trace-row code { display: none; }.browser-actions { align-items: flex-start; flex-direction: column; }
    .final-cta { padding-block: 100px; }.final-cta > div { align-items: stretch; flex-direction: column; }.final-cta .button { width: 100%; }
  }
</style>
